# Flutter App（MVP 占位）

按架构定稿：Flutter 3.x + Drift(SQLite) + 离线优先。

本目录仅提供包名与 feature 分层占位，待本机安装 Flutter SDK 后执行：

```bash
flutter create --org com.ibd --project-name ibd_mobile .
# 将 lib/ 下 feature 骨架迁入
```

MVP features：lab / medication / injection / symptom / parse / reminders
