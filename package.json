{
  "name": "ibd-management",
  "private": true,
  "version": "0.1.0",
  "description": "肠安通 IBD 病程管理 monorepo（MVP 骨架）",
  "packageManager": "pnpm@9.15.0",
  "scripts": {
    "build:api": "pnpm --filter @ibd/api build",
    "start:api": "pnpm --filter @ibd/api start:dev",
    "typecheck": "pnpm -r --if-present typecheck"
  },
  "engines": {
    "node": ">=20"
  }
}
