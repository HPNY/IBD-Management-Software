---
feature: local-first
status: designed
updated: 2026-09-13
branch: main
---

# IBDers 本地优先改造方案

## Report

## [S1] Problem

当前 MVP 把 **Postgres 当权威存储**：检验/用药/注射/症状默认写库，JWT 登录为强制门槛。这与两条产品原则冲突：

1. **个人医疗数据默认只存本机，未得到用户授权不得上传**  
2. **App 是个人终端应用**：默认用应用内标识，**不强制登录**

需要把权威数据迁回设备，服务端降级为「可选能力」（云端 AI 解析、可选同步/备份、Skill 社区、医生端），并重新划分「永不上传 / 授权后上传 / 可匿名上云」边界。

## [S2] Design — 原则与目标架构

### S2.1 硬约束

| 约束 | 含义 |
|------|------|
| 本地权威 | 病程事实源在 App 本地 DB（Drift/SQLite）；服务端无默认副本 |
| 默认离线 | 无网络可完成：记症状、记用药、看历史、看已下载报告 |
| 最小上传 | 仅在用户对**单次操作**明确同意后上传；可随时「关同步 / 删云端」 |
| 无强制登录 | 首启生成 `app_user_uuid`，无需手机号/微信即可用满核心功能 |
| 可迁移优先 | 换机靠「本地导出 + 用户自选云备份 / 手工导入」，不靠设备硬件 ID |

### S2.2 目标架构

```text
┌─────────────────────────────────────────────┐
│                 Flutter App                  │
│  app_user_uuid（本地生成，非 IMEI/IDFA）      │
│  Drift/SQLite：患者、检验、用药、注射、症状…  │
│  本地加密（SQLCipher / 字段级）               │
│  本地提醒（flutter_local_notifications）      │
└───────────────┬─────────────────────────────┘
                │ 仅授权时
                ▼
     ┌──────────────────────┐
     │  可选云端能力（Opt-in） │
     │  ① 单次 PDF 解析      │ ← 临时上传，可「用完即删」
     │  ② 可选同步/备份      │ ← 用户开关 + 端到端加密
     │  ③ Skill 社区/医生端  │ ← 显式分享，不含原始病历默认集
     └──────────────────────┘
```

服务端角色从「主库」变为：

- **Parse Worker**：有网且用户点「AI 解析」时的可选算力  
- **同步服务**：用户打开同步后的多设备副本（非权威）  
- **社区/医生端**：分享与授权查看  

### S2.3 数据分类（上传策略）

| 类别 | 例子 | 默认 | 授权后 |
|------|------|------|--------|
| A 病程本体 | 检验值、日记、用药、手术文字、原始 PDF | **永不自动上传** | 仅「同步/备份」开关开启后加密上传 |
| B 解析任务 | 本次 PDF 字节 | 本地 | 「AI 解析」单次同意；支持解析后删除原件 |
| C 派生模板 | ParseSkill 正则 JSON | 可本地生成 | 可匿名贡献社区（去掉医院病历字段） |
| D 诊断崩溃 | 崩溃堆栈、性能 | 可选 | 默认关，设置里打开 |

### S2.4 身份模型

| 模式 | 标识 | 服务端 |
|------|------|--------|
| 本地（默认） | `app_user_uuid`（首启 randomUUID，存本地） | 不要求账号 |
| 同步/备份（可选） | 同一 uuid + 用户绑定手机/微信后签发 JWT | 恢复与多端 |
| 医生端 | 患者扫码授权，短期凭证 | 只读授权子集 |

**禁止**：IMEI、IDFA、ANDROID_ID 作为用户主键。

## [S3] 改动面清单

### A. App（Flutter）— 主战场

| 模块 | 改动 | 优先级 |
|------|------|--------|
| 本地库 | 引入 **Drift**：schema 与 API 实体对齐（lab/med/injection/symptom/parse_job/…） | P0 |
| 身份 | `LocalIdentityService`：生成/持久化 `app_user_uuid`；登录页改为「可选」 | P0 |
| 鉴权 | 业务页改为读本地库；去掉「未登录 401 门禁」 | P0 |
| 检验录入/直传解析 | 先写本地；上传 PDF 需单独确认弹窗 | P0 |
| 解析结果 | 落本地 labs；Skill 可在本地由确认结果生成 | P0 |
| 用药/注射/症状 | 全量本地 CRUD；注射提醒用本地通知 | P0 |
| 加密 | SQLCipher 或敏感字段 AES；密钥放 Keystore/Keychain | P1 |
| 同步开关 | 设置页：关闭时零网络；开启才绑账号 + 增量同步 | P1 |
| 导出/导入 | JSON 导出（用户发起）；换机导入 | P1 |
| 云端解析 | 「用 AI 解析」授权流 + 解析后删除云端原件选项 | P1 |

### B. 服务端（Nest / Worker）

| 模块 | 改动 | 优先级 |
|------|------|--------|
| 默认策略 | 文档与部署默认 **不要求登录即可解析**；病历写库仅 sync 路径 | P0 |
| Parse API | 「匿名/会话上传」：短时上传令牌（TTL），可不绑长期账号 | P1 |
| 同步 API | 以 `app_user_uuid` + 绑定账号做端到端密文同步；服务端不解密病历 | P1 |
| 实体保留 | labs/meds 等表保留，但语义改为「云副本」非权威 | P1 |
| Skill 社区 | 只收 Skill JSON，不收原始报告 | P2 |
| 医生端 | 授权子集 + 审计 | P2 |

### C. 文档 / 合规

- 更新架构 spec：本地权威 + 可选同步  
- 隐私说明：收集什么、何时上传、如何删除云端数据  
- 默认关闭分析/崩溃上报  

## [S4] 分期

### Phase 0 — 决策冻结（0.5–1 天）

- [ ] 确认 A/B/C/D 分类与「用完即删」产品文案  
- [ ] 确认同步采用：E2E 密文同步 vs 服务端可读副本（建议 **E2E 密文**）  
- [ ] 确认 MVP 是否完全砍掉「强制登录」

**验收**：书面勾选本节，无 TBD。

### Phase 1 — App 本地权威（核心，约 1–2 周）

- [ ] Drift schema + 迁移（对齐现有 API 实体）  
- [ ] `app_user_uuid` 身份；删除登录门禁  
- [ ] 检验/用药/注射/症状/日记改为本地读写  
- [ ] 注射提醒改为本地通知  
- [ ] 解析：本地 Skill 快路径；AI 解析弹「是否上传本文件」  
- [ ] 无网冒烟：飞行模式下完成打卡 + 用药 + 看历史  

**验收**：断网全流程可用；抓包无病历上传（未开同步、未点 AI 解析）。

### Phase 2 — 可选云能力（约 1–2 周）

- [ ] 解析上传：短时令牌 + 可选「解析后删除」  
- [ ] 同步/备份：设置开关 → 绑定手机/微信 → 密文增量同步  
- [ ] 服务端表语义改为 cloud replica；清理「无登录写 lab」路径  
- [ ] 导出 JSON / 换机导入  

**验收**：关同步则服务器无新病历行；开同步可在第二台设备恢复。

### Phase 3 — 加固与产品扩展（约 1 周+）

- [ ] SQLCipher / 字段加密  
- [ ] 隐私设置：分析开关、云端数据删除  
- [ ] Skill 社区（匿名模板，无病历）  
- [ ] 医生端授权（P2）  

**验收**：安全清单过一遍；删除账号可清云副本。

## [S5] 与现状差距（摘）

| 现状 | 目标 |
|------|------|
| JWT 全局守卫，未登录 401 | 默认无账号；JWT 仅同步/云解析 |
| Nest labs/meds 为权威 | Drift 为权威 |
| 解析成功即写 parse_jobs/labs | 解析结果回写 App；云 jobs 可短期保留 |
| FCM/厂商待做 | 注射提醒先本地通知，推送后置 |

## [S6] Out of Scope

- 不在本方案内实现代码  
- 不选定具体 SQLCipher 插件供应商合同  
- 医生端详细 UX  
- 等保/伦理审查材料清单  

## Tasks

- [x] T1: 冻结数据分类与同步加密模型 — acceptance: §S4 Phase 0 无未决项 (covers: S2, S3)
- [x] T2: Flutter 本地库 + app_user_uuid — acceptance: 断网可增删改查病程 (covers: A, Phase 1; depends: T1)
- [x] T3: 去掉登录门禁，业务页本地优先 — acceptance: 冷启动无登录可用 (covers: A; depends: T2)
- [x] T4: AI 解析单次授权上传 — acceptance: 未同意不上传 PDF (covers: A, B; depends: T2)
- [x] T5: 可选同步/备份（E2E 密文） — acceptance: 开关关=无云写；开=双端可恢复 (covers: B, Phase 2; depends: T3)
- [ ] T6: 本地加密与云端删除 — acceptance: 敏感字段加密；可删云副本 (covers: Phase 3; depends: T5)
